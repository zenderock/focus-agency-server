# Flask / JWT
SECRET_KEY=CHANGE_ME_DEV_SECRET
TOKEN_EXPIRY=3600

# Callback (Supabase / Webhook) – si nécessaire
# Jeton Bearer utilisé pour les callbacks de succès/erreur après conversion
CALLBACK_BEARER=

# MeSOMB
MESOMB_APP_KEY=
MESOMB_API_KEY=
MESOMB_API_SECRET=

# CORS (CSV)
FOCUST_ALLOWED_ORIGINS=https://focustagency.com,https://trainer.focustagency.com,https://learner.focustagency.com,http://localhost:3000

# Download Token v2
# Si true, le token doit aussi matcher le filename exact de l'URL de download.
# Laissez à false pour continuer sans binding filename (moins sécurisé).
DOWNLOAD_TOKEN_REQUIRE_FILENAME=false
